# Environment variables — never commit secrets
.env

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.claude/
# Track the uniplug-flo plugin (custom Claude Code plugin for this repo).
!.claude/
!.claude/plugins/
!.claude/plugins/uniplug-flo/
!.claude/plugins/uniplug-flo/**
stocktake/
scratch/

# one-time legal source bundle — do not commit
/files.zip

# Design handoff bundle (reference only — never commit the zip or the unzipped prototype)
/design/handoff/
/unconventional-signup-flow-design/
/Unconventional signup flow design-handoff.zip
/*design-handoff.zip
/*Dashbaord-handoff.zip
/uniplug-dashbaord/
/uniplug-student-dashbaord/

# Local-only dev affordances (NEVER commit — temporary login bypass + local seed accounts)
/src/routes/dev-login.tsx
/supabase/dev-seeds/p10-verify-local-login.sql
/supabase/dev-seeds/run-p10-verify-local-login.sh
